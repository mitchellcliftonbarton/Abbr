# Abbr

Wordpress theme: `/abbr-wp-theme`
Nuxt app: `/nuxt

## WP Theme Install via Symlink

You can symlink the theme into a local WP development site:

```
ln -s /path/to/repository/themes/abbr /path/to/local-wp/wp-content/themes/
```
